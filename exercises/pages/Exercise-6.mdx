---
name: Ex. 6 - Render props
order: 6
---

# Ex. 6 - Render props

## Instructions

The application handles errors, but not in a fine way. The desired behavior would be that the error is displayed instead of the movie list and not the entire application.

The strategy is to isolate the movie list and fetching the data in a component so that it can be included in a Catch.

- Create a "render props" component in MovieSearch in `src/containers/MovieDb.js`
- Integrate this component in `App` and add a` Catch` around

### Expected result

When the search field is empty, I get an error instead of the movie list.

<iframe
  src="https://codesandbox.io/embed/github/smooth-code/react-hooks-workshop/tree/end-exercise-6/?fontsize=14&view=preview"
  style={{
    width: '100%',
    height: 500,
    border: 0,
    borderRadius: 4,
    overflow: 'hidden',
  }}
  sandbox="allow-modals allow-forms allow-popups allow-scripts allow-same-origin"
/>

## Help

### Render props: implementation

```js
export function Counter({ initialValue, children }) {
  const count = useCounter(initialValue)
  return children(count)
}
```

### Render props: usage

```js
function Example() {
  return <Counter>{count => <div>{count}</div>}</Counter>
}
```
